{
  "name": "ritzvincentculanag.github.io",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-watch": "sass --watch --update --style=expanded assets/sass:assets/css",
    "sass-build": "sass --no-source-map --style=compressed assets/sass:assets/css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "sass": "^1.83.1"
  }
}
